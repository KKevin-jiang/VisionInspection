# 百力通通机视觉检测软件 — 需求与开发计划

> 公司：百力通（重庆）发动机有限公司  
> 文档日期：2026-06-09  
> 状态：开发中 v1.6  
> 整合：相机 IO 联调清单（2026-06-10）  
> 计划：相机参数手动控制（2026-06-10）

---

## 一、项目背景

- **产品**：小型风冷式发动机（通机）
- **机型**：10V3 / 12V3 / 19V3 / 25V3 / 25E3 / 1062 / 130G / 13R2 等（用户可自定义新增）
- **检测目的**：检测发动机是否涂胶 / 是否安装垫片（不同机型检测项目不同）
- **生产线通信**：RFID 系统与上位机通信，PC 从上位机 SQL Server 获取托盘机型信息

---

## 二、技术选型

| 类别 | 技术 |
|------|------|
| UI 框架 | PyQt5（暗色现代简约风格，Design Token 体系） |
| 设计系统 | Modern Enterprise Design System（4px 网格、完整状态覆盖、class 选择器） |
| 工业相机 | 海康威视 MVS SDK（Python 接口） |
| IO 触发 | Line0 输入（三菱 PLC）/ Line1 输出（高电平=合格，低电平=不合格） |
| 上位机通信 | pyodbc 连接 SQL Server（写结果）/ HTTP JSON API（自动换型查机型） |
| 图像处理 | OpenCV + NumPy |
| AI 推理 | Ultralytics YOLOv8（仅加载已有 `.pt` 文件，不含训练工具） |
| 报表导出 | pandas + openpyxl |

---

## 三、运行环境

| 项目 | 要求 |
|------|------|
| Python | 3.11（推荐，YOLOv8 + PyTorch 兼容性最佳） |
| 虚拟环境 | venv（`py -3.11 -m venv venv`） |
| VC++ 运行库 | Visual C++ 2015-2022 Redistributable（PyTorch 依赖） |
| ODBC 驱动 | Microsoft ODBC Driver 17 for SQL Server |
| 相机驱动 | 海康 MVS 客户端，设置 `MVCAM_COMMON_RUNENV` 环境变量 |
| 无 GPU | 支持纯 CPU 推理（`--index-url https://download.pytorch.org/whl/cpu`） |

### 安装命令

```bash
py -3.11 -m venv venv
venv\Scripts\pip install PyQt5 opencv-python numpy pyodbc pandas openpyxl
venv\Scripts\pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
venv\Scripts\pip install ultralytics
```

---

## 四、硬件 IO 说明与联调

### 4.1 接线确认

```
三菱 PLC ──── Line0（输入）──→ 工业相机（触发采集）
工业相机 ──── Line1（输出）──→ 三菱 PLC（合格/不合格信号）

Line1 逻辑：
  合格   → 高电平，持续 500ms（默认，可配置）后恢复低电平
  不合格 → 保持低电平
```

| 步骤 | 检查项 | 说明 |
|------|--------|------|
| 1 | PLC 触发线 → 相机 `Line0+` | 确认接在相机对应输入端，公共地和电平规格与相机型号一致 |
| 2 | 相机 `Line1+` → PLC 输入端 | NG/OK 回传线，确认 PLC 输入端与相机输出类型匹配 |
| 3 | IO 电平与供电 | 核对相机说明书：IO 电平标准、是否需要外接电源、是否为光耦输入输出 |

### 4.2 相机节点配置（MVS 客户端）

| 步骤 | 节点 | 设定值 | 说明 |
|------|------|--------|------|
| 1 | `TriggerMode` | `On` | 启用外部触发 |
| 2 | `TriggerSource` | `Line0` | 触发源为 Line0 输入 |
| 3 | `TriggerActivation` | `RisingEdge` | 与 PLC 输出边沿一致，通常为上升沿 |
| 4 | `LineDebouncerTime` | `50 µs` | 防抖滤波，防止误触发 |
| 5 | `LineSelector` | `Line1` → `LineMode = Output` | Line1 设为输出模式 |
| 6 | `LineSource` | `UserOutput0` | 映射到用户可编程输出节点（视相机型号而定） |

### 4.3 软件侧 Line0/Line1 参数

**Line0 触发参数**：
- `TriggerMode = On`
- `TriggerSource = Line0`
- `TriggerActivation = RisingEdge`
- `LineDebouncerTime = 50 µs`

**Line1 输出控制**（MVS SDK）：
- `LineSelector = Line1` → `LineMode = Output`
- 通过 `UserOutputSelector = UserOutput0` + `UserOutputValue = True/False` 控制电平

**配方侧检查**：
- 配方 `trigger_mode` 设为 `plc_external`
- 在配方编辑页"检测与IO"中确认 `相机输出线` 为 `Line1`，脉宽与延时符合现场节拍
- 主界面启动 IO 监听后，状态应停留在"等待 Line0"
- PLC 发出一个有效脉冲后，软件应立即进入检测并刷新图像与结果

### 4.4 结果回传验证

| 步骤 | 测试内容 | 验收标准 |
|------|----------|----------|
| 1 | 制造 OK 样本 | 确认 **不会误输出** Line1 脉冲 |
| 2 | 制造 NG 样本 | 确认检测完成后 Line1 **有脉冲输出** |
| 3 | 脉宽测量 | 用示波器或 PLC 监控，脉宽与设定值一致（默认 500ms） |
| 4 | 无信号排查 | 若 PLC 收不到信号，优先检查 `LineSource`、输出类型、电平和公共地 |

### 4.5 常见 IO 故障排查

| 故障现象 | 可能原因 | 排查步骤 |
|----------|----------|----------|
| 一直等待触发 | `TriggerSource` 未设为 `Line0` | 1. 检查 MVS 中 TriggerSource 配置<br>2. 检查 PLC 脉冲宽度是否过窄 |
| 相机能采图但软件不更新 | 未启动 IO 监听或相机被其他程序占用 | 1. 确认主界面已启动 IO 监听<br>2. 关闭 MVS 客户端等其他占用相机的程序 |
| NG 不回传 / 无 Line1 输出 | Line1 输出未正确配置 | 1. 查 `Line1` 是否支持输出模式<br>2. 查 `UserOutput0` 是否被该相机型号支持<br>3. 查 `LineSource` 映射是否正确 |
| 节拍波动大 | 多种因素叠加 | 1. 排除 PLC 触发抖动<br>2. 检查曝光时间是否过长<br>3. 检查网络丢包<br>4. 检查磁盘保存是否阻塞 |

---

## 五、上位机数据库

| 项目 | 值 |
|------|----|
| 服务器 | `192.168.0.101\SQL201` |
| 数据库 | `BLT` |
| 账号 | `sa` / `123456` |
| 数据表 | `[BLT].[dbo].[T_SerialNo]` |
| 机型字段 | `MachineType` |
| 流水号字段 | `ActSerialNo` |

**查询逻辑**：
```sql
SELECT TOP 1 MachineType
FROM [BLT].[dbo].[T_SerialNo]
WHERE ActSerialNo = ?
```

---

## 六、换型功能

支持两种换型模式，可在设置中配置，也可在主界面工具栏快速切换：

### 6.1 自动换型（默认）

- 流水号有效性校验：非空、非 `"0"`、长度恰好为 10，否则丢弃等待下次
- 每次 Line0 触发或手动输入流水号时，PC 通过 **HTTP API** (`POST /api/v1/crankshaft/model-by-serial`) 查询机型，不再直接查 SQL Server
- API 返回完整机型号（如 `10V3AABB1234`），取**前 4 位**作为配方键（`10V3`）匹配本地配方文件
- 幂等去重：同一流水号仅处理一次，避免重复换型
- 若机型与当前不同 → 自动加载新配方 → 主界面状态栏显示换型通知（"自动换型：163 → 208"）
- 机型显示区为**只读**，操作员无法手动修改
- `app_config.json` 字段：`"switch_mode": "auto"`，API 地址在 `crankshaft_api.base_url` 配置

### 6.2 手动换型

- 软件**忽略** SQL Server 的机型字段，不发起查询
- 主界面右侧显示机型 **下拉选择框**（列出所有已配置配方）+ "确认切换"按钮
- 切换时弹出 `QMessageBox` 二次确认，防止误操作
- 选定后配方**锁定**，直到操作员再次手动切换
- `app_config.json` 字段：`"switch_mode": "manual"`

---

## 七、检测算法

每个机型的配方独立配置检测算法，两种算法可按机型混用：

### 7.1 传统二值化（默认）

1. 对配置的 ROI 区域裁剪图像
2. **灰度二值化**（三种模式可选）：
   - `otsu`：Otsu 自适应阈值，自动计算最优阈值
   - `fixed`：固定阈值，`gray > threshold_value` 的像素为白色
   - `range`：灰度区间，`gray_low ≤ gray ≤ gray_high` 的像素为白色
3. **目标明暗（polarity）**：
   - `bright`：目标为亮区（白色）不变
   - `dark`：目标为暗区，自动反转二值图
4. **手动反转（invert）**：可手动反转二值结果
5. 形态学开运算去噪（morph_kernel 可调）
6. `cv2.connectedComponentsWithStats` 统计连通域面积
7. **面积判定**：
   - `max_area = 0`：`detected_area ≥ min_area` 即合格
   - `max_area > 0`：`min_area ≤ detected_area ≤ max_area` 才合格（排除过大噪点/反光）

### 7.2 AI 模型（YOLOv8）

1. 加载 `.pt` 文件（`ultralytics.YOLO`）
2. 对 ROI 区域推理
3. 按类别 + 置信度阈值判断
4. 不含训练/标注工具，仅推理

---

## 八、项目文件结构

```
vision_inspection/
├── main.py                          # 程序入口，初始化 QApplication
├── requirements.txt                 # 依赖列表（版本锁定）
│
├── config/
│   ├── app_config.json              # 系统配置（DB、相机、工位ID、换型模式、Line1脉宽）
│   └── recipes/                     # 各机型配方 JSON（文件名为机型号前4位）
│       ├── 10V3.json
│       ├── 12V3.json
│       ├── 19V3.json
│       ├── 25V3.json
│       ├── 1062.json
│       ├── 130G.json
│       ├── 13R2.json
│       └── ...（其余机型同理）
│
├── core/
│   ├── __init__.py
│   ├── camera_manager.py            # MVS SDK 封装，IO 触发，Line1 控制
│   ├── detector.py                  # 检测引擎（二值化 + AI 双模式）
│   ├── database.py                  # SQL Server 操作（写结果+心跳重连）
│   ├── crankshaft_api.py            # 曲轴工位 HTTP API 客户端（流水号校验/重试/幂等）
│   └── storage.py                   # 图片/CSV/Excel 存储
│
├── models/
│   ├── recipe.py                    # Recipe 数据类
│   └── detection_result.py          # DetectionResult 数据类
│
├── ui/
│   ├── __init__.py
│   ├── main_window.py               # 主窗口（完整检测流水线集成）
│   ├── widgets/
│   │   ├── live_view.py             # 实时画面 QLabel
│   │   ├── roi_canvas.py            # 交互式 ROI 画布（鼠标拖拽框选/选中/调整）
│   │   ├── result_panel.py          # 结果大字展示（绿/红）
│   │   └── stats_panel.py           # 今日统计卡片
│   ├── dialogs/
│   │   ├── settings_dialog.py       # 设置（相机/数据库/存储/换型模式）
│   │   └── recipe_editor.py         # 配方编辑器（含实时二值化预览）
│   └── styles/
│       └── dark_theme.qss           # 暗色主题（Design Token 体系）
│
└── utils/
    ├── logger.py
    └── image_utils.py
```

---

## 九、app_config.json 关键字段

```json
{
  "crankshaft_api": {
    "base_url": "http://192.168.0.101:8080",
    "timeout_ms": 1500,
    "source": "vision-inspection"
  },
  "database": {
    "server": "192.168.0.101\\SQL201",
    "database": "BLT",
    "username": "sa",
    "password": "123456",
    "serial_table": "T_SerialNo",
    "serial_field": "ActSerialNo",
    "model_field": "MachineType",
    "result_table": "T_VisionResult",
    "station_id": "ST001"
  },
  "camera": {
    "trigger_source": "Line0",
    "trigger_activation": "RisingEdge",
    "debounce_us": 50,
    "exposure_us": 5000,
    "gain_raw": 0,
    "gamma": 1.0,
    "frame_rate": 30.0,
    "digital_gain": 1.0
  },
  "io": {
    "line1_pass_duration_ms": 500
  },
  "switch_mode": "auto",
  "storage": {
    "image_root": "D:\\VisionImages",
    "save_pass_images": true,
    "log_root": "D:\\VisionLogs"
  }
}
```

---

## 十、主界面布局

```
┌──────────────────────────────────────────────────────────────────┐
│  顶栏：视觉检测系统 │ 工位 相机 PLC 配方 模板 模式 节拍 │ 时间  │
├───────────────────────────────┬──────────────────────────────────┤
│                               │  配方控制                        │
│                               │  [配方选择 ▼] [刷新配方] [配方编辑]│
│                               │  当前模板 / 当前产品              │
│                               │  [新增配方] [复制配方] [删除配方] │
│    实时画面                    │  [启动 IO 监听] [停止] [硬件触发] │
│   （叠加 ROI 框               ├──────────────────────────────────┤
│    和检测标注）               │  检测结果                        │
│                               │  ✅ 合  格  /  ❌ 不 合 格       │
│                               │  耗时 / 触发时间 / NG数量 / 连续NG│
│                               ├──────────────────────────────────┤
│                               │  相机参数                        │
│                               │  曝光时间：[5000] μs             │
│                               │  增益：[0.0] dB  Gamma：[1.0]   │
│                               │  帧率：[30.0] fps               │
│                               │  [读取当前值]    [应用]           │
│                               ├──────────────────────────────────┤
│                               │  并行对照                        │
│                               │  主判定 / 并行算法 / 对照摘要     │
│                               ├──────────────────────────────────┤
│                               │  ROI 检测结果（表格）             │
│                               ├──────────────────────────────────┤
│                               │  统计信息（今日）│    操作        │
│  系统日志（表格）              │  OK/NG/OK率/    │  手动测试      │
│                               │  连续NG/最近保存 │  保存图像      │
│                               │                  │  查看记录      │
│                               │                  │  清除报警      │
├───────────────────────────────┴──────────────────────────────────┤
│  底部状态栏：系统状态 │ 版本 │ 当前用户 │ 消息                    │
└──────────────────────────────────────────────────────────────────┘
```

**布局说明**：
- **顶栏**：深色背景，依次显示系统标题、工位/相机/PLC/配方/模板/模式/节拍状态、实时时钟
- **左侧面板**（5/8 宽度）：
  - 上部 4/6：实时画面（叠加 ROI 框和检测标注），含缩放/适应/保存工具栏
  - 下部 2/6：系统日志表格（时间/级别/消息三列）
- **右侧面板**（3/8 宽度，可滚动）：
  - 配方控制：配方下拉选择器 + 刷新/编辑按钮、模板/产品信息、新增/复制/删除、IO 监听启停
  - 检测结果：大字 OK/NG + 耗时/触发时间/NG数量/连续NG 详情
  - 相机参数：曝光时间/增益/Gamma/帧率 SpinBox + 读取/应用按钮
  - 并行对照：主判定/并行算法 + 对照摘要
  - ROI 检测结果：各 ROI 判定明细表格（启用/名称/阈值/算法/分值/状态）
  - 底部行：统计信息卡片（OK数/NG数/OK率/连续NG/最近保存）+ 操作按钮（手动测试/保存图像/查看记录/清除报警）
- **底部状态栏**：深色背景，系统状态/版本号/当前用户/就绪消息

**手动换型模式时**，右侧配方控制区增加机型下拉选择框和确认切换按钮。

---

## 十一、配方数据结构（Recipe）

```json
{
  "model_name": "10V3",
  "detection_type": "glue",
  "algorithm": "binarization",
  "roi_list": [
    { "x": 120, "y": 80, "w": 200, "h": 150, "label": "油封涂胶区" }
  ],
  "binarization": {
    "threshold_method": "otsu",
    "threshold_value": 128,
    "gray_low": 0,
    "gray_high": 255,
    "polarity": "bright",
    "invert": false,
    "min_area": 500,
    "max_area": 0,
    "morph_kernel": 3
  },
  "ai": {
    "model_path": "",
    "confidence": 0.6,
    "classes": ["glue", "no_glue"]
  },
  "line1_duration_ms": 500
}
```

### 二值化参数详解

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `threshold_method` | string | `"otsu"` | 阈值方式：`otsu`（自动）/ `fixed`（固定）/ `range`（灰度区间） |
| `threshold_value` | int | 128 | 固定阈值 0-255，仅 `fixed` 模式生效 |
| `gray_low` | int | 0 | 灰度下限，仅 `range` 模式生效 |
| `gray_high` | int | 255 | 灰度高限，仅 `range` 模式生效 |
| `polarity` | string | `"bright"` | 目标明暗：`bright`（目标亮）/ `dark`（目标暗） |
| `invert` | bool | false | 手动反转二值图 |
| `min_area` | int | 500 | 连通域最小面积（下限） |
| `max_area` | int | 0 | 连通域最大面积（上限），0=无上限 |
| `morph_kernel` | int | 3 | 形态学开运算核大小（奇数） |

`detection_type` 可选值：`glue` / `gasket` / `both`  
`algorithm` 可选值：`binarization` / `ai`

---

## 十二、数据存储

### 图片存储路径
```
{image_root}/{YYYYMMDD}/{MachineType}/{ActSerialNo}_{PASS/FAIL}_{HHmmss}.jpg
```

### CSV 日志字段
```
timestamp, serial_no, machine_type, result, score, algorithm, image_path
```

### Excel 报表
按日期范围汇总，自动生成工作表，包含统计图表（合格率折线图）。

---

## 十三、相机参数手动控制

### 13.1 功能目标

在主界面提供相机基础参数的实时读取/设置面板，替代海康 MVS 客户端的手动调参流程。现场调试时，操作员无需切换到 MVS 即可完成曝光时间、增益等常用参数的调整。

### 13.2 可控参数

通过 GenICam 标准节点名访问相机参数，按相机型号自动探测可用参数：

| 参数 | 节点名 | 类型 | 典型范围 | 说明 |
|------|--------|------|----------|------|
| 曝光时间 | `ExposureTime` | Float | 20 ~ 1,000,000 μs | 影响图像亮度，值越大图像越亮 |
| 模拟增益 | `GainRaw` | Float/Int | 0 ~ 24 dB | 信号放大倍数，过高引入噪点 |
| Gamma | `Gamma` | Float | 0.1 ~ 4.0 | 灰度非线性变换 |
| 帧率 | `AcquisitionFrameRate` | Float | 1 ~ 相机最大帧率 | 采集帧率控制 |
| 数字增益 | `DigitalGain` | Float | 1.0 ~ 16.0 | 数字域增益，部分相机不支持 |

> **注意**：实际可用参数取决于相机型号。`HikCameraClient` 通过 `MV_CC_GetFloatValue` / `MV_CC_SetFloatValue` 通用接口读写，对不支持的参数静默跳过并记录 WARN 日志。

### 13.3 UI 位置与布局

主界面右侧面板，在「检测结果」与「统计信息」之间新增 `QGroupBox("相机参数")`：

```
┌─ 相机参数 ──────────────────────┐
│  曝光时间：[  5000] μs  ↕      │
│  增益：    [   0.0] dB  ↕      │
│  Gamma：   [   1.0]     ↕      │
│  帧率：    [  30.0] fps ↕      │
│                                 │
│  [读取当前值]    [应用]         │
└─────────────────────────────────┘
```

### 13.4 交互方式

- **参数控件**：`QDoubleSpinBox`，范围和步长根据参数类型设定
  - 曝光时间：20 ~ 1,000,000 μs，步长 100
  - 增益：0 ~ 24 dB，步长 0.1
  - Gamma：0.1 ~ 4.0，步长 0.1
  - 帧率：1 ~ 120 fps，步长 1
- **读取当前值**：调用 `camera_controller.get_camera_params()` 刷新所有 SpinBox 显示
- **应用**：将当前面板值逐个通过 `camera_controller.set_camera_param()` 下发到相机
- **自动应用**（可选优化）：SpinBox 值改变后 300ms debounce 自动下发，减少手动点击
- **软件启动时**：自动从 `app_config.json` 恢复上次参数，并尝试写入相机

### 13.5 联动锁定逻辑

- **IO 监听运行中**：所有相机参数控件置灰（`setEnabled(False)`），防止运行时修改影响在线检测
- **相机未连接**：参数控件置灰，面板显示"相机未连接"
- **参数读取失败**：对应 SpinBox 保留上次值，状态栏提示读取失败信息

### 13.6 持久化

在 `app_config.json` 的 `camera` 节点中存储最近一次设置的参数值（详见第九章），软件启动时恢复到相机，软件正常退出时保存当前值。

---

## 十四、开发阶段（历史，已完成）

| 阶段 | 内容 | 关键文件 |
|------|------|---------|
| Phase 1 | 项目脚手架、数据模型、默认配方 | `models/`, `config/` |
| Phase 2 | 相机管理、数据库、检测引擎、存储 | `core/` |
| Phase 3 | 主界面、样式表、设置对话框、配方编辑器 | `ui/` |
| Phase 4 | 主流程集成（触发→换型→检测→IO→存储） | `main_window.py` |
| Phase 5 | requirements.txt、打包部署说明 | 根目录 |

---

## 十五、验证步骤

1. **相机测试**：无 PLC 时改用软件触发（`TriggerSource=Software`），验证采图正常
2. **数据库测试**：设置界面点击"测试连接"，验证 `SELECT MachineType FROM T_SerialNo WHERE ActSerialNo='xxx'` 返回正确
3. **IO 测试**：用万用表测 Line1，合格时测到高电平持续 ~500ms 后回低
4. **IO 联调**（详见第四章完整清单）：
   - 接线确认：PLC ↔ 相机 Line0/Line1
   - 相机节点确认：TriggerMode / TriggerSource / TriggerActivation / LineSelector
   - 软件侧检查：配方 trigger_mode / IO 监听启动 / 触发响应
   - 结果回传验证：OK/NG 样本测试、示波器脉宽测量
5. **自动换型测试**：启动上位机 HTTP 服务，输入有效流水号（10位非0），验证软件通过 API 获取完整机型号并提取前4位匹配配方（如返回 `10V3AABB1234` → 加载 `10V3.json`）；输入无效流水号验证被状态栏提示拒绝
6. **手动换型测试**：切换至手动模式，选择机型，确认换型锁定，再次 Line0 触发不改变机型
7. **二值化调试**：
   - 拍摄或加载图片 → 鼠标拖拽框选 ROI → 表格同步更新
   - 切换阈值方式（Otsu / 固定 / 灰度区间）
   - 调节目标明暗（bright / dark）+ 反转
   - 设置面积上下限（min_area / max_area）
   - 右侧面板实时预览：原图 | Otsu二值 | 形态学开运算 | 连通域标注
   - 预览显示 PASS/FAIL + 检测面积 + 阈值详情
8. **AI 推理测试**：加载 `.pt` 文件，对样品图验证推理返回正确类别
9. **报表验证**：运行 10 条后导出 Excel，验证字段和图片路径完整
10. **相机参数手动控制**：
    - 连接相机后，主界面「相机参数」面板应正确读取并显示当前曝光时间、增益、Gamma 值
    - 修改曝光时间后采图，图像亮度应有对应变化（增大曝光 → 图像变亮）
    - 参数修改后点击"应用"，再次读取应确认已生效
    - 关闭软件再重启，上次设置的参数值应从 `app_config.json` 恢复
    - 启动 IO 监听后，相机参数控件应自动锁定（置灰不可操作）
    - 停止 IO 监听后，参数控件应恢复可编辑状态

---

## 十六、依赖清单（requirements.txt）

```
PyQt5>=5.15.9
opencv-python>=4.8.0
numpy>=1.24.0,<2        # PyTorch 2.1 兼容，限制 <2
pyodbc>=5.0.0
requests>=2.31.0        # 曲轴工位 HTTP API 客户端
ultralytics>=8.0.0
pandas>=2.0.0
openpyxl>=3.1.0
```

> **注意**：
> - Python 版本：3.11（3.12+ PyTorch 兼容性问题，3.13 不支持）
> - 需安装 Microsoft ODBC Driver for SQL Server（建议 ODBC Driver 17）
> - 需安装 MVS 相机驱动，并设置 `MVCAM_COMMON_RUNENV` 环境变量
> - 需安装 Visual C++ 2015-2022 Redistributable（PyTorch DLL 依赖）
> - 无 GPU 机器使用 CPU 版 PyTorch：`pip install torch --index-url https://download.pytorch.org/whl/cpu`

---

## 十七、更新日志

### v1.4 (2026-06-09) — 曲轴工位自动换型 HTTP API + 4位机型命名

- 新增 `core/crankshaft_api.py`：
  - `validate_serial_no()`：流水号有效性校验（非空、非"0"、长度10）
  - `CrankshaftApiClient.get_machine_type()`：`POST /api/v1/crankshaft/model-by-serial`，每次携带 UUID `requestId`
  - 重试策略：code 1003 最多重试 3 次，间隔 200 / 300 / 500 ms
  - 幂等去重：`_last_processed_serial`，同一流水号不重复处理
  - 结构化日志：每次调用记录 requestId / serialNo / responseCode / machineType / costMs / retryIndex
  - `check_health()`：`GET /api/v1/health` 健康检查
- `app_config.json` 新增 `crankshaft_api` 节点（base_url / timeout_ms / source）
- `main_window.py` 自动换型改为调用 HTTP API，移除直连 SQL Server 查机型逻辑
- 机型命名规则变更：改用 API 返回机型号的**前 4 位**作为配方键（如 `10V3AABB1234` → `10V3`）
  - `main_window._recipe_key()` 负责截取，与存储/日志中保留的完整机型号解耦
  - 配方文件统一重命名为 4 位前缀格式：`10V3.json`、`12V3.json`、`19V3.json`、`25V3.json` 等
- 流水号输入框增加格式校验，无效流水号在状态栏提示并拒绝处理
- `requirements.txt` 新增 `requests>=2.31.0`

### v1.3 (2026-06-08) — Design Token 主题 + 运行环境

- 基于 Modern Enterprise Design System 重写 `dark_theme.qss`：
  - 完整 Design Token 体系（DARK_BACKGROUND #0B121F 等）
  - 4px 网格间距系统
  - 完整状态覆盖（:hover / :pressed / :focus / :disabled）
  - class 选择器体系（`[class="primary"]` 等）
  - 组件规范：按钮/输入框/表格/标签/状态栏/滚动条
- 调整"大津法"显示为"自动阈值"（更直观）
- 预览面板标题中文化（原图/二值化/开运算/连通域）
- 确定运行环境：Python 3.11 + venv + CPU 版 PyTorch 2.1.2
- 解决 PyTorch DLL 加载问题（VC++ 运行库 + 降级到 2.1.2）

### v1.2 (2026-06-08) — 全面中文化

- 所有 UI 界面文字全部中文化（按钮、标签、菜单、状态栏、提示信息）
- 所有下拉框显示中文（内部保留英文字段值，保证配置文件兼容性）：
  - 检测类型：涂胶检测 / 垫片检测 / 两者都检
  - 算法：二值化 / AI 模型
  - 阈值方式：大津法（自动）/ 固定阈值 / 灰度区间
  - 目标明暗：亮目标 / 暗目标
  - 触发源：线0触发 / 软件触发 / 自由运行
  - 触发沿：上升沿 / 下降沿
  - 换型模式：自动换型 / 手动换型
- 全部日志消息中文化（相机、数据库、检测器、存储、主窗口）
- 检测结果详情中文化（面积、阈值方式、目标类型等）
- 配方验证错误消息中文化
- ROI 默认标签：检测区_1, 检测区_2, ...
- 预览结果：合格/不合格

### v1.1 (2026-06-08) — 配方编辑器可视化增强

- 新增 `ui/widgets/roi_canvas.py`：交互式 ROI 画布，支持鼠标拖拽框选/选中/移动/调整大小
- 重写 `ui/dialogs/recipe_editor.py`：
  - 嵌入 RoiCanvas + [拍摄] [加载图片] 按钮
  - ROI 画布 ↔ 表格双向同步
  - 实时二值化预览：4 面板（原图 | 二值 | 开运算 | 连通域标注）
- 二值化参数增强：
  - 三种阈值方式：Otsu 自动 / Fixed 固定 / Range 灰度区间
  - 目标明暗选择（bright/dark）+ 反转
  - 面积上下限（min_area / max_area）
  - 灰度上下限（gray_low / gray_high，仅 Range 模式）

### v1.0 (2026-06-08) — 初始版本

- 完整的项目脚手架、数据模型、默认配方（6个机型）
- 海康 MVS 相机 SDK 封装（Line0 触发 / Line1 输出 / 软件触发）
- 检测引擎：二值化 + YOLOv8 AI
- SQL Server 数据库（心跳重连 / 查询机型 / 写结果）
- 图片存储 + CSV 日志 + Excel 报表（含合格率折线图）
- PyQt5 暗色主题 UI：主窗口 / 设置对话框 / 配方编辑器
- 自动换型 / 手动换型双模式

### v1.6 (2026-06-10) — 相机参数手动控制

- 新增「相机参数手动控制」功能模块，在主界面右侧面板直接调节相机基础参数
- **扩展 `infrastructure/camera/hik_camera_client.py`**：
  - 新增 `get_float_param()` / `set_float_param()` / `get_int_param()` / `set_int_param()` 通用参数读写方法
  - 底层调用 `MV_CC_GetFloatValue` / `MV_CC_SetFloatValue` / `MV_CC_GetIntValue` / `MV_CC_SetIntValue`
- **扩展 `application/services/camera_service.py`**：
  - 新增 `get_camera_params()`：批量读取当前相机的曝光时间、增益、Gamma、帧率等
  - 新增 `set_camera_param()`：按节点名设置单个参数值
- **扩展 `application/controllers/camera_controller.py`**：新增透传方法
- **修改 `ui/main_window.py`**：
  - 右侧面板新增「相机参数」GroupBox，含曝光时间/增益/Gamma/帧率 SpinBox
  - 「读取当前值」按钮刷新相机实时参数
  - 「应用」按钮将修改后的参数下发到相机
  - IO 监听运行期间相机参数控件自动锁定（置灰禁用）
- `app_config.json` 新增相机参数字段（`exposure_us` / `gain_raw` / `gamma` / `frame_rate` / `digital_gain`）
- 软件启动时自动从 `app_config.json` 恢复上次的相机参数
- 第十章主界面布局更新：右侧面板新增「相机参数」区域
- 第十五章验证步骤新增第 10 条：相机参数手动控制验证

### v1.5 (2026-06-10) — 整合相机 IO 联调清单

- **第四章扩展**：将 `camera_io_commissioning.md` 完整整合为「硬件 IO 说明与联调」
  - 4.1 接线确认：PLC ↔ 相机 Line0/Line1 接线检查表
  - 4.2 相机节点配置：MVS 客户端 6 步配置清单
  - 4.3 软件侧检查：配方 trigger_mode / IO 监听 / 触发响应
  - 4.4 结果回传验证：OK/NG 样本测试 + 示波器脉宽测量
  - 4.5 常见 IO 故障排查：4 类典型故障的现象→原因→排查步骤表
- 第十四章验证步骤中新增 IO 联调条目，交叉引用第四章
